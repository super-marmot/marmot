# PII action verifier rubric

The action chip must be deterministic and local. It must visibly exist in the
Quick actions screen, use a semantic shield icon, redact common identifiers,
and return a copyable typed result without requiring a model or network.

The verifier is intentionally narrow: names, addresses, and arbitrary
identifiers need a later reviewable extraction capability and must not be
claimed as covered by this milestone.
